package com.huidi.spring.boot.initializer.config;

import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest
public class pictureConfigTest {

    /*@Autowired
    private PictureConfig pictureConfig;
    @Test
    public void addResourceHandlers() throws Exception {
        String imagesPath=pictureConfig.getMImagesPath();
        imagesPath = imagesPath.substring(0, imagesPath.lastIndexOf("/"))+"/images/";
        System.out.println(imagesPath);
        System.currentTimeMillis();
    }
*/
}